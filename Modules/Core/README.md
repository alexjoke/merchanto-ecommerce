# Core Module

Shared **contracts** (`app/Contracts/`) and **DTOs** (`app/Data/`) for cross-module communication.

- No Eloquent models
- No business services
- Feature modules implement interfaces here and bind them in their own service provider
